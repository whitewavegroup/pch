import Link from 'next/link';export default function ProviderCard({p}:{p:any}){return(<div className='card'><div style={{display:'flex',justifyContent:'space-between'}}><b>{p.name}</b><span>{p.country}</span></div><div>{(p.services||[]).map((ps:any)=>ps.service?.key).join(' • ')}</div><Link href={'/providers/'+p.slug}>Ver perfil</Link></div>)}