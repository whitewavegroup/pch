export default async function handler(req, res){
  // TODO: Implement Supabase CRUD depending on req.method
  // GET: list, POST: insert, PUT: update, DELETE: remove
  return res.status(200).json({ ok: true, message: 'Replace with Supabase CRUD for listings.' });
}
