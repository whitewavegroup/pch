export default async function handler(req, res){
  // TODO: GET reviews by listing, POST new review (verified lead only)
  return res.status(200).json({ ok: true, message: 'Replace with Supabase reviews read/write.' });
}
